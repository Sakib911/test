// Stub implementation for KYC service
// Remove or replace with actual implementation as needed

export const useUploadDocument = () => {
  return {
    mutate: () => {},
    mutateAsync: async (file?: File) => ({ data: null }),
    isPending: false,
    isError: false,
    error: null,
  }
}
